# План реализации: Ветки магий и целенаправленное изучение

> Статус: **РЕАЛИЗОВАНО** (Все этапы 1–5 завершены)  
> Связанные документы: [`docs/agents/magic-system.md`](../agents/magic-system.md), [`server/arena/MagicService.ts`](../../server/arena/MagicService.ts), [`shared/actions/branches.ts`](../../shared/actions/branches.ts)

---

## 1. Цели и мотивация

1. **Отказ от рандома («всё подряд»)**:
   - В текущей системе игрок нажимает «Изучить круг N», после чего сервис [`MagicService.learnMagic`](../../server/arena/MagicService.ts) с шансом 60% выдает случайное заклинание из общего пула класса.
   - Поскольку пул заклинаний небольшой (1–2 заклинания на круг внутри ветки), случайный выбор лишен смысла и раздражает игрока промахами.
2. **Формирование билдов (Специализация)**:
   - Игрок может развивать персонажа максимум в **1–2 ветки** (из 3 доступных классу).
   - Третья ветка блокируется, стимулируя разнообразие билдов (например: Стихийный маг, Маг крови/некромант, Маг контроля/защиты; Хил-жрец, Защитник-жрец, Инквизитор-жрец).
3. **Целенаправленное изучение (Targeted Learning)**:
   - Игрок видит дерево/список заклинаний своей ветки и целенаправленно вкладывает очки `bonus` в конкретные магии со **100% шансом успеха**.

---

## 2. Каталог веток и распределение заклинаний

У каждого магического класса есть **3 ветки**.

### Маг (`m`)

| Ветка | Название | Тематика | Заклинания по кругам |
|---|---|---|---|
| `elements` | **Стихии (Элементализм)** | Прямой и AoE урон огнем, холодом, молнией | **К1**: `1ma` Магическая стрела, `1md` Ледяное прикосновение<br>**К3**: `3ma` Цепная молния, `3md` Огненный дождь<br>**К5**: `5mb` Огненный шар, `5md` Ледяное копьё<br>**К6**: `6ma` Дикая молния, `6mb` Огненная буря |
| `darkness` | **Тьма (Чернокнижие)** | Яды, кислоты, вампиризм, истощение, некромантия | **К1**: `1mf` Ядовитое дыхание<br>**К2**: `2mc` Вампиризм<br>**К3**: `3mb` Кислотный плевок<br>**К4**: `4mb` Усыхание (Истощение)<br>**К7**: `7ma` Жертва, `7mb` Призвать мёртвых, `7mc` Маг. вампиризм, `7me` Вампирическая аура |
| `arcana` | **Искажение (Ограждение и Разум)** | Защитные ауры, щиты, иллюзии, контроль (сон, сайленс, глюки) | **К1**: `1mb` Магический доспех, `1me` Малая аура, `1mc` Слабое лечение<br>**К2**: `2ma` Сильная аура, `2mb` Глюки<br>**К3**: `3mc` Безумие<br>**К4**: `4ma` Зеркальный щит, `4mc` Усыпление, `4md` Безмолвие, `4me` Щит праха<br>**К5**: `5ma` Сфера, `5mc` Бунт богов<br>**К6**: `6mc` Сотонинские глюки, `6md` Зеркальное отражение |

### Жрец (`p`)

| Ветка | Название | Тематика | Заклинания по кругам |
|---|---|---|---|
| `holy` | **Свет (Исцеление)** | Прямое и массовое лечение, регенерация, воскрешение | **К1**: `1pc` Слабое лечение<br>**К2**: `2pc` Среднее лечение<br>**К3**: `3pb` Вторая жизнь, `3pc` Сильное лечение<br>**К5**: `5pa` Единение духа, `5pb` Святое оружие<br>**К6**: `6pc` Массовое лечение<br>**К7**: `7pa` Жизненная сила |
| `protection` | **Опека (Благословения)** | Баффы статов, каменная кожа, барьеры, снятие негативных эффектов | **К1**: `1pa` Благословение, `1pf` Каменная кожа<br>**К2**: `2pa` Средняя аура, `2pb` Магическая защита<br>**К3**: `3pd` Экзорцизм<br>**К4**: `4pc` Магическая стена<br>**К5**: `5pd` Кошачий коготь<br>**К7**: `7pc` Молитва |
| `inquisition` | **Инквизиция (Кара)** | Проклятия, замедление, сжигание маны/энергии, возврат боли | **К1**: `1pb` Проклятие, `1pd` Опутывание, `1pe` Камнепад<br>**К2**: `2pd` Снятие магии (Рассеивание), `2pe` Паралич<br>**К3**: `3pa` Затмение<br>**К4**: `4pb` Дух тела, `4pd` Физическая печаль (Скорбь), `4pe` Анафема<br>**К5**: `5pc` Чума<br>**К6**: `6pa` Магическое опустошение, `6pb` Энергетическое опустошение<br>**К7**: `7pd` Магическая печаль |

---

## 3. 8-й круг: Истинная магия подклассов (Перспектива / Улучшение класса)

8-й круг представляет собой **Истинную магию** («Prestige Magic»). Она открывается при улучшении класса персонажа (получении второго класса без сброса уровня). Подробная документация подклассов и навыков: [`docs/agents/subclasses.md`](../agents/subclasses.md).

### 3.1. Магические подклассы и их заклинания

| Подкласс | Комбинация | Код | Заклинание | Механика |
|---|---|---|---|---|
| **Епископ** | Жрец + Жрец (`p+p`) | `8ppa`<br>`8ppb` | **Святость**<br>**Святой обряд** | Полная неуязвимость к физ. атакам и прямой магии (спадает при атаке)<br>Карающий святой урон |
| **Отшельник** | Жрец + Маг (`p+m`) | `8pma`<br>`8pmb` | **Ураганный ветер**<br>**Подмена** | Урон ветром со срезом атаки цели на 10–30<br>Обманная магия подмены цели |
| **Хаотик** | Маг + Жрец (`m+p`) | `8mpa`<br>`8mpb` | **Апокалипсис**<br>**Армагеддон** | Комбо: общие глюки + анафема + неснимаемый холод на 5 ходов<br>Глобальный взрыв по всей арене |
| **Архимаг** | Маг + Маг (`m+m`) | `8mma`<br>`8mmb` | **Мозговая атака**<br>**Ледяной шторм** | Тяжелейший ментальный урон разуму цели<br>Ледяной вихрь по врагам |
| **Аббат** | Воин + Жрец (`w+p`) | `8wpa`<br>`8wpb` | **Жажда крови**<br>**Клонирование** | Резкое увеличение атаки цели<br>Создание своего двойника в бою |
| **Элементалист** | Воин + Маг (`w+m`) | `8wma` | **Ярость** | Увеличение атакующего урона яростью стихий |
| **Друид** | Лучник + Маг (`l+m`) | `8lma` | **Святая защита** | Щит святости и периодическое исцеление |
| **Монах** | Лучник + Жрец (`l+p`) | `8lpa`<br>`8lpb` | **Слабость**<br>**Кровавая аура** | Сильное ослабление атаки цели<br>Аура крови |

### 3.2. Как 8-й круг вписывается в систему веток

1. **Вне лимита 2 веток**: 8-й круг **не расходует слоты базовых веток**. Он является надстройкой над специализацией персонажа.
2. **Привязка к подклассу, а не к ветке**: Доступ к магии 8-го круга даётся строго при наличии соответствующего улучшенного подкласса персонажа.
3. **Архитектурный задел (Future-proofing)**:
   - В `shared/actions` предусмотреть для `Magic` опциональные метаданные:
     ```typescript
     subclassReq?: SubclassId; // например, 'archmage' | 'bishop' | 'chaotic' | ...
     profComboReq?: [CharacterClass, CharacterClass]; // ['m', 'm'], ['m', 'p']...
     ```
   - В модель `Character` предусмотреть возможность хранения подкласса:
     ```typescript
     subclass?: string; // идентификатор улучшенного подкласса
     ```
   - В `MagicService` заложить проверку требований к подклассу при `lvl === 8`.

---

## 4. Правила выбора веток и прокачки (Вариант А)

1. **Лимит веток**: Персонаж может иметь **максимум 2 активные ветки** (`magicBranches`).
2. **Слоты веток**:
   - **Слот 1 (Основная ветка)**: Доступен сразу (с 1-го уровня персонажа). Игрок выбирает одну из трех веток своего класса.
   - **Слот 2 (Вторая ветка)**: Открывается при достижении **5-го уровня персонажа** (на этом уровне персонажу становится доступен 3-й круг магии: `ceil(5/2) = 3`).
   - **Слот 3 (Третья ветка)**: Остается заблокированным. Заклинания третьей ветки изучить нельзя.
3. **Целенаправленная прокачка заклинаний**:
   - Игрок кликает на конкретное заклинание из своих активных веток.
   - Проверяются условия:
     - Заклинание принадлежит одной из выбранных веток.
     - Уровень персонажа достаточен для круга заклинания: `canLearnMagic(character.lvl, magic.lvl)`.
     - Текущий уровень заклинания меньше 3: `(character.magics[magic.name] ?? 0) < 3`.
     - У персонажа хватает очков `bonus`: `character.bonus >= getLearnMagicCost(magic.lvl)`.
   - При подтверждении списываются очки `bonus`, уровень магии повышается на +1 со **100% шансом**.
4. **Сброс веток (Респек)**:
   - Игрок может нажать кнопку «Сбросить магии».
   - Сброс очищает `magicBranches`, обнуляет `character.magics` и возвращает все потраченные очки `bonus` (за фиксированную плату золотом / арканитом).

---

## 5. Архитектура изменений

### 5.1. Слой Shared (`packages/shared` или `shared/`)

1. **Типы веток (`shared/actions/branches.ts`)**:
   ```typescript
   export type MageBranch = 'elements' | 'darkness' | 'arcana';
   export type PriestBranch = 'holy' | 'protection' | 'inquisition';
   export type MagicBranchId = MageBranch | PriestBranch;

   // Задел под подклассы 8-го круга (улучшение класса):
   export type SubclassId =
     | 'bishop'       // p+p Епископ
     | 'hermit'       // p+m Отшельник
     | 'chaotic'      // m+p Хаотик
     | 'archmage'     // m+m Архимаг
     | 'abbot'        // w+p Аббат
     | 'elementalist' // w+m Элементалист
     | 'druid'        // l+m Друид
     | 'monk';        // l+p Монах

   export interface MagicBranchMeta {
     id: MagicBranchId;
     name: string;
     description: string;
     icon: string;
     prof: 'm' | 'p';
   }

   export const SECOND_BRANCH_MIN_CHAR_LVL = 5;
   ```
2. **Метаданные заклинания и гибридные школы**:
   - Поле `branches?: MagicBranchId[]` прямо в параметрах магии (`MagicArgs` в `server/arena/Constuructors/MagicConstructor.ts` и в конкретных файлах `server/arena/magics/`).
   - Заклинание может принадлежать **сразу нескольким школам (гибридное)**. Персонажу достаточно иметь **хотя бы одну** из этих веток, чтобы выучить заклинание:
     - `dispel`: `['protection', 'inquisition']` (снятие дебаффов со своих / снятие баффов с врагов)
     - `secondLife`: `['holy', 'protection']` (святое воскрешение / защитный бафф спасения от смерти)
     - `exorcism`: `['protection', 'inquisition']` (очищение соратников / изгнание скверны)
     - `lightHeal`: `['holy', 'protection']` для Жреца; `['arcana']` для Мага
     - `bodySpirit`: `['inquisition', 'holy']` (карающее выжигание HP врага + пополнение маны)
     - `frostTouch`: `['elements', 'darkness']` (стихийный урон холодом + периодический DoT увядания)
     - `blight`: `['darkness', 'arcana']` (некротический срез HP + ментальное истощение)
     - `vampirism`: `['darkness', 'arcana']` (высасывание здоровья + перенос энергии)
   - В интерфейсе `Magic` возвращается `branches: MagicBranchId[]` и `branch: MagicBranchId`.

### 5.2. Слой Базы Данных (`server/models/character.ts`)

- Добавить в схему `Character` поле:
  ```typescript
  magicBranches: {
    type: [String],
    default: [],
  }
  ```
- Обеспечить обратную совместимость для существующих персонажей: если персонаж уже имеет магии, при первом входе его ветки либо автоопределяются по изученным магам, либо дается бесплатный разовый респек.

### 5.3. Серверная бизнес-логика (`server/arena/MagicService.ts`)

- Добавить методы:
  - `selectBranch(character: CharacterService, branchId: MagicBranchId)`:
    - Проверка принадлежности ветки классу персонажа.
    - Проверка лимита: макс 2 ветки. Если выбирается 2-я — проверка уровня персонажа `>= SECOND_BRANCH_MIN_CHAR_LVL`.
  - `learnSpecificMagic(character: CharacterService, magicName: string)`:
    - Проверка наличия заклинания в `arena.magics`.
    - Проверка принадлежности ветке из `character.magicBranches` (или подклассу для 8-го круга).
    - Проверка `canLearnMagic(character.lvl, magic.lvl)`.
    - Проверка `(character.magics[magicName] ?? 0) < MAX_MAGIC_LVL`.
    - Списание `bonus` (`getLearnMagicCost(magic.lvl)`).
    - Вызов `character.learnMagic(magicName, currentLvl + 1)`.
    - 100% шанс (без `learnChance()`).
  - `resetMagics(character: CharacterService)`:
    - Подсчет всех потраченных очков `bonus`.
    - Очистка `character.magics = {}`.
    - Очистка `character.magicBranches = []`.
    - Возврат очков `bonus`.

### 5.4. HTTP API (`server/server/magic.ts`)

- `POST /magic/branch` — выбор ветки (body: `{ branch: MagicBranchId }`).
- `POST /magic/learn/:magicName` — изучение конкретного заклинания.
- `POST /magic/reset` — сброс всех веток и магий с возвратом бонусов.
- `GET /magic/branches` — статус веток персонажа (выбранные, доступные для выбора, заблокированные, прогресс).

### 5.5. Клиентский интерфейс (`client/src/routes/(app)/character/magics/`)

- **Экран без выбранной ветки**:
  - Приветственный выбор 1-й специализации с описанием и иконками 3 веток.
- **Древовидный / карточный экран магий**:
  - Вкладки или аккордеоны выбранных веток.
  - Список заклинаний, сгруппированный по кругам (1..7).
  - На каждом заклинании:
    - Название, иконка, описание, стоимость маны.
    - Текущий уровень (звездочки / индикатор `1/3`, `2/3`, `3/3`).
    - Кнопка «Улучшить» с точной ценой в `bonus💡` (активна только при выполнении условий).
  - Слот 2-й ветки:
    - Если `lvl < 5`: заблокирован с плашкой «Откроется на 5-м уровне».
    - Если `lvl >= 5` и не выбрана: кнопка «Выбрать вторую специализацию».
  - Заблокированная 3-я ветка:
    - Отображается серым/замочком с подсказкой о лимите специализации.
  - Кнопка «Сброс магий» внизу экрана с модальным окном подтверждения.

---

## 6. Итоги реализации (Все этапы завершены)

1. [x] **Шаг 1 (Shared + Models)**:
   - В `@fwo/shared` добавлены типы `MageBranch`, `PriestBranch`, `MagicBranchId`, метаданные `MAGIC_BRANCHES`, лимиты `MAX_MAGIC_BRANCHES = 2`, `SECOND_BRANCH_MIN_CHAR_LVL = 5` и задел под 16 подклассов улучшения класса (`SUBCLASSES`).
   - В схему `Character` (shared и mongoose) добавлено поле `magicBranches: string[]`.
   - В `Magic` добавлен массив `branches?: MagicBranchId[]`.
   - Поле `branches` добавлено напрямую в `MagicArgs` и в параметры всех 37 реализованных файлов магий в `server/arena/magics/`.
   - Полностью удален промежуточный словарь `MAGIC_TO_BRANCHES`: каждое заклинание самодостаточно декларирует свои специализации.
2. [x] **Шаг 2 (Server Business Logic)**:
   - В `CharacterService` добавлены геттер `magicBranches`, методы `setMagicBranches`, `resetMagics`, сохранение в базу и выгрузка клиенту.
   - В `CharacterResources` добавлена поддержка добавления `bonus` для корректного 100% возврата очков при респеке.
   - В `MagicService` реализованы:
     - `selectBranch(character, branchId)` — выбор специализации с валидацией класса, лимита в 2 ветки и требования 5-го уровня для 2-го слота.
     - `learnSpecificMagic(character, magicName)` — детерминированное изучение (100% шанс успеха, списание `bonus = lvl ** 2`, проверка круга магии, лимит 3 ранга).
     - `resetMagics(character)` — полный сброс изученных магий и веток со 100% возвратом потраченных бонусов.
     - `getBranchesInfo(character)` — метаданные, статус выбора, доступность выбора 2-й ветки.
     - `getBranchMagics(character, branchId)` — список заклинаний конкретной ветки с поддержкой гибридных магий.
   - **Гибридные заклинания (мульти-школы)**: заклинания могут принадлежать нескольким школам (`dispel`, `secondLife`, `exorcism`, `lightHeal`, `bodySpirit`, `frostTouch`, `blight`, `vampirism`, `mediumAura`). Для изучения достаточно иметь хотя бы одну из школ заклинания.
3. [x] **Шаг 3 (Server HTTP API)**:
   - В `server/server/magic.ts` зарегистрированы RPC-эндпоинты:
     - `GET /branches`
     - `POST /branch`
     - `POST /learn/:name`
     - `POST /reset`
     - `GET /branch/:branchId`
4. [x] **Шаг 4 (Client UI — Svelte 5)**:
   - Созданы раннеры `selectBranch`, `learnSpecificMagic`, `resetMagics`.
   - Обновлена страница `client/src/routes/(app)/character/magics/+page.svelte`:
     - 3 режима: «Магии» (каталог изученных с подробностями и школами), «Ветки» (выбор 1-й и 2-й ветки, индикатор слотов, прогресс), «Изучение» (целенаправленная прокачка с отображением цен в бонусах, текущего ранга 1-3 и бейджей «гибрид»).
     - Кнопка сброса магий с предупреждением и подтверждением.
5. [x] **Шаг 5 (Тестирование и верификация)**:
   - Написаны 17 unit-тестов в `server/arena/MagicService.test.ts` (выбор веток, гейт 5-го уровня, лимит 2 веток, целенаправленное изучение, максимальный ранг 3, полный возврат бонусов, гибридные школы).
   - Все 148 тестов сервера и сборка фронтенда проходят без ошибок.
